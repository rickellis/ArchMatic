ORDER OF EVENTS

* Install Arch 
* Install reflector and update mirrorlist
* Install git

* Install Xorg (do I need this?)
* Install XFCE and display manager. LightDM?

* Install video drivers (research)
* Install Network packages
* Download WifiVPN and get online

* Install audio packages
* Install bluetooth packages
* Install AVAHI Network discovery
* Install printer drivers
* Configure auto-login (see ArchMergeD/800-autologin.sh)


* Install core software via pacman

* Create home folders
* Install Dotfiles



# To enable Wifi on Virtualbox:

First, get the name of your connection using:

    #   ip link

Should be something like `enp0s3`. Then run:

    #   sudo ip link set dev enp0s3 up
    #   sudo dhcpcd enp0s3


